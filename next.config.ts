import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: "/cypso-labs-front-end-v5",
  output: "export",  // <=== enables static exports
  /* config options here */
};

export default nextConfig;
